---
title: About
slug: "/about"
---

import React from "react";
import "antd/dist/antd.css";
import { Timeline, Tag } from "antd";
import { ClockCircleOutlined } from "@ant-design/icons";
import { TimelineItems, ActivityTypeColorMap } from "./timeline-items";

Hi!

Welcome to my blog. Here is my education and work timeline at a glance.

<Timeline mode="alternate">
  {TimelineItems.map((ti, idx) =>
    idx % 2 === 0 ? (
      <Timeline.Item key={ti.date} style={{ fontSize: "20px" }}>
        <Tag color={ActivityTypeColorMap[ti.type]}>
          <i>{ti.date}</i>
        </Tag>{" "}
        - <b>{ti.activity}</b>
      </Timeline.Item>
    ) : (
      <Timeline.Item key={ti.date} style={{ fontSize: "20px" }}>
        <b>{ti.activity}</b> -{" "}
        <Tag color={ActivityTypeColorMap[ti.type]}>
          <i>{ti.date}</i>
        </Tag>
      </Timeline.Item>
    )
  )}
</Timeline>
